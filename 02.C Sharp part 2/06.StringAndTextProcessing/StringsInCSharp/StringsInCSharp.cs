//Problem 1. Strings in C#.

//Describe the strings in C#.
//What is typical for the string data type?
//Describe the most important methods of the String class.

using System;

class StringsInCSharp
{
    static void Main()
    {
        /* - Strings are sequences of characters.
        * - Each character is a Unicode symbol.
        * - They are represented by the string data type.
        * - Strings are stored in the dynamic memory.
        * - Strings are immutable.
        * -  Methods: Lenght, this[], Compare(str1, str2),
         * IndexOf(str), LastIndexOf(str), Substring(startIndex, length),
         * Replace(oldStr, newStr), Remove(startIndex, length), ToLower(),
         * ToUpper(), Trim().
        */
    }
}
